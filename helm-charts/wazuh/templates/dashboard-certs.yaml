apiVersion: v1
kind: Secret
metadata:
  name: {{ template "app.name" . }}-dashboard-certs
type: Opaque
data:
  cert.pem: # key base64 encoded
  key.pem: # key base64 encoded
  root-ca.pem: # key base64 encoded
