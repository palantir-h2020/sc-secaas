run:
    description: "Action to execute a command into container"
    params:
        cmd:
            description: "Command tu be executed"
            type: string
            default: ""
    required:
      - cmd
